version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 18
        - corepack enable
        - node --version
        - pnpm i -f
    build:
      commands:
        - pnpm build
  artifacts:
    baseDirectory: .amplify
    files:
      - "**/*"
  # cache:
  #   paths:
  #     - node_modules/**/*
